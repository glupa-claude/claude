# SSH keys

Public keys only — private keys live in `~/.ssh` on the Mac and are never committed.

- `ubuntu-vm-id_ed25519.pub` — access to the Parallels Ubuntu Server VM (`glupa@10.211.55.7`, key-only auth).
